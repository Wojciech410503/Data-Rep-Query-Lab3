// Define a functional component named Create
const Create = () => {
    // Return the JSX to be rendered
    return (
        <div>
            {/* Render a main heading with the text "This is my create component!" */}
            <h1>This is my create component!</h1>
        </div>
    );
}

// Export the Create component so it can be used in other parts of the application
export default Create;
